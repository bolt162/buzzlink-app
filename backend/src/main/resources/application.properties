# Application
spring.application.name=buzzlink-backend
server.port=8080

# Database Configuration (PostgreSQL)
# For production, use PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/buzzlink
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# CORS Configuration is handled in SecurityConfig.java
# Do not configure CORS here to avoid conflicts

# WebSocket Configuration
spring.websocket.allowed-origins=http://localhost:3000,http://localhost:3001,http://184.169.147.113:3000

# Actuator endpoints for Prometheus
management.endpoints.web.exposure.include=health,info,prometheus,metrics
management.metrics.export.prometheus.enabled=true
management.endpoint.health.show-details=always

# Kafka Configuration (design/stub only - not fully wired)
# In production, these would point to actual Kafka brokers
spring.kafka.bootstrap-servers=localhost:9092
spring.kafka.producer.key-serializer=org.apache.kafka.common.serialization.StringSerializer
spring.kafka.producer.value-serializer=org.springframework.kafka.support.serializer.JsonSerializer

# Email Configuration (Gmail SMTP - for development)
# To enable email sending:
# 1. Create a Gmail App Password: https://myaccount.google.com/apppasswords
# 2. Uncomment and configure the settings below
# 3. Replace YOUR_EMAIL and YOUR_APP_PASSWORD with your credentials

# Email enabled with Gmail SMTP
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=ash16299@gmail.com
spring.mail.password=mdaq lzuw lwsa rphu
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# Frontend URL (for email links)
app.frontend.url=http://localhost:3000

# Logging
logging.level.com.buzzlink=DEBUG
logging.level.org.springframework.web=INFO
